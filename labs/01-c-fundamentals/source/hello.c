#include <stdio.h>    // Preprocessor directive to include external library files
                      // Standard Input Output Header file for printf and scanf functions

int main() {    // The main entry point function where program execution begins

    printf("This is my first c code\n"); // Prints output data or text to the screen
    return 0;   // Ends the main function and tells the OS that the program ran successfully
}
