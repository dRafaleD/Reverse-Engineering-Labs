#include <stdio.h>
#include <string.h>

int main() {
    char name[] = "Rafale";
    char code[] = "RE_LAB_11";

    printf("Name: %s\n", name);
    printf("Code: %s\n", code);
    printf("Name length: %zu\n", strlen(name));

    return 0;
}
